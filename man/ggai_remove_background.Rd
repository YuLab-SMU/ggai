% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_postprocess.R
\name{ggai_remove_background}
\alias{ggai_remove_background}
\title{Remove a flat chroma-key background and return an alpha PNG}
\usage{
ggai_remove_background(
  path,
  output_path = NULL,
  key_color = NULL,
  transparent_threshold = 12/255,
  opaque_threshold = 220/255,
  despill = TRUE,
  crop = TRUE,
  pad = 8L,
  alpha_noise_floor = 8/255
)
}
\arguments{
\item{path}{Path to the source image (PNG or JPEG).}

\item{output_path}{Optional output path. Defaults to a sibling file with
\verb{_nobg.png} suffix in the same directory.}

\item{key_color}{Optional hex string (e.g. \code{"#00ff00"}). When \code{NULL}
(the default), the key colour is sampled from the image border as the
per-channel median.}

\item{transparent_threshold}{Chebyshev distance below which pixels are
fully transparent. Default \code{12/255} matches the official skill.}

\item{opaque_threshold}{Chebyshev distance at or above which pixels are
fully opaque. Default \code{220/255} matches the official skill's
recommended invocation.}

\item{despill}{Whether to cap key-colour spill on partially-transparent
pixels by clamping the key channels to the strongest non-key channel.}

\item{crop}{Whether to crop the output to the alpha bounding box.}

\item{pad}{Pixel padding to leave around the cropped subject.}

\item{alpha_noise_floor}{Alpha values at or below this (and above 0) are
snapped to 0 to suppress speckle. Default \code{8/255}.}
}
\value{
Path to the written PNG with alpha channel. \code{NULL} if the image
could not be read or the \code{png} package is unavailable.
}
\description{
Ports the algorithm used by the official Codex \code{imagegen} skill
(\code{remove_chroma_key.py}): sample the background key colour from the image
border, key off Chebyshev distance to that colour, build a smooth alpha
ramp between the transparent and opaque thresholds, combine with a
key-channel dominance penalty to catch antialiased edges, then despill
the key colour from partially-transparent pixels.
}
\details{
This is the default post-processing step after generating an asset on a
chroma-key background — the recommended workflow for \code{gpt-image-2},
which does not honour the API's \code{background = "transparent"} parameter.
}
