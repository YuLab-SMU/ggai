% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact.R
\name{ggai_save_artifact}
\alias{ggai_save_artifact}
\title{Persist a \code{ggai_artifact} to disk}
\usage{
ggai_save_artifact(artifact, output_dir, prefix = NULL, overwrite = FALSE)
}
\arguments{
\item{artifact}{A \code{ggai_artifact}.}

\item{output_dir}{Directory to write into. Created if missing.}

\item{prefix}{Filename prefix. Defaults to \code{artifact$id}.}

\item{overwrite}{If \code{FALSE} (default), error if any target file already
exists. Set \code{TRUE} to clobber.}
}
\value{
A named list of saved paths (\code{code}, \code{manifest}, plus one entry per
rendered format).
}
\description{
Writes the artifact's reproducer code (\verb{<prefix>.R}), all entries in its
\code{rendered} list (one file per format), and a JSON manifest
(\verb{<prefix>.json}) into \code{output_dir}.
}
\details{
Pure side-effecting verb. Returns a named list of the paths written so the
caller (typically the agent) can reference them later.
}
