% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent.R
\name{ggai_run_agent}
\alias{ggai_run_agent}
\title{Run a ggai agent on a goal}
\usage{
ggai_run_agent(agent, goal, session = NULL, model = NULL, max_steps = 10L, ...)
}
\arguments{
\item{agent}{An aisdk \code{Agent} (e.g. from \code{ggai_create_agent()}).}

\item{goal}{Character scalar describing the figure task.}

\item{session}{Optional aisdk \code{ChatSession} for shared state across calls.}

\item{model}{Optional model override.}

\item{max_steps}{Maximum ReAct loop iterations. Default \code{10}.}

\item{...}{Passed through to \code{Agent$run()}.}
}
\value{
A list with the aisdk result and the produced artifact:
\itemize{
\item \code{result} — the value returned by \code{agent$run()}.
\item \code{artifact} — the most-recent \code{ggai_artifact} from the shared state, or \code{NULL} if the agent didn't produce one.
\item \code{artifacts} — all artifacts produced this run, indexed by id.
}
}
\description{
Thin wrapper around \code{Agent$run()} that surfaces the produced artifact in
the returned result.
}
