% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai_bridge.R
\name{ggai_capability_status}
\alias{ggai_capability_status}
\title{Report which figure-generation capabilities are configured}
\usage{
ggai_capability_status(probe = FALSE, refresh = FALSE, ttl = 60L, timeout = 5L)
}
\arguments{
\item{probe}{If \code{TRUE}, perform a live HEAD reachability check per
configured capability. Default \code{FALSE} (config-only, no network).}

\item{refresh}{If \code{TRUE}, bust the probe cache and re-probe.}

\item{ttl}{Probe cache time-to-live in seconds. Default \code{60}.}

\item{timeout}{Per-probe request timeout in seconds. Default \code{5}.}
}
\value{
A named list as described above.
}
\description{
Returns a snapshot the agent (and skills) consult to decide between
\strong{code mode} (write ggplot / grid R via \code{ggai_execute_r}) and \strong{image-model
mode} (call \code{ggai_generate_image()} / \code{polish_figure()}).
}
\details{
With \code{probe = FALSE} (the default), the check is config-only — fast,
offline, and based on \code{ggai_default_models()} plus the presence of the
provider's API-key env var. A capability is \code{available = TRUE} when the
identifier resolves and the key env is set.

With \code{probe = TRUE}, each configured capability is verified by issuing a
lightweight \code{HEAD} request against the provider's endpoint. A \code{404} means
the route is missing (the configured proxy doesn't serve that capability);
any other response (\code{200}, \code{401}, \code{405}, ...) means the route exists and
the capability is reachable. Probe results are cached in-process for \code{ttl}
seconds so repeated calls within the same turn don't pay the round-trip.
Use \code{refresh = TRUE} to bust the cache.

Fields:
\itemize{
\item \code{language_model}, \code{language_provider}, \code{language_available}
\item \code{image_model}, \code{image_provider}, \code{image_available}
\item \code{probed} — \code{TRUE} when at least one probe ran this call.
\item \code{probe_results} — per-capability \code{list(status, reachable, route, cached, error)} when \code{probe = TRUE}.
\item \code{summary} — short human-readable text snippet suitable for the agent to log or reason over.
}

Edge cases:
\itemize{
\item Providers that do not require an API key (e.g. local Ollama) currently report \code{available = FALSE}.
\item Providers without a known route mapping (\code{anthropic} for images, \code{gemini}, \code{bailian}) are skipped under \code{probe = TRUE}; the config-only result is used.
\item Network errors or timeouts during probe collapse to \code{reachable = FALSE}.
}
}
\examples{
\dontrun{
status <- ggai_capability_status()
if (!status$image_available) message("Image model not configured.")

# Verify the endpoint actually serves the image route:
status <- ggai_capability_status(probe = TRUE)
if (!status$image_available) message("Image route unreachable; using code mode.")
}
}
