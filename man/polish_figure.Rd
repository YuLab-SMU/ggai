% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure_polish.R
\name{polish_figure}
\alias{polish_figure}
\title{Redraw a data-grounded figure into a polished final image}
\usage{
polish_figure(
  x,
  instruction = NULL,
  image_model = NULL,
  registry = NULL,
  candidate_count = 1L,
  output_dir = file.path(getwd(), "demo_outputs"),
  prefix = "figure_polish",
  width = NULL,
  height = NULL,
  dpi = 300,
  output_format = c("png", "jpeg", "webp"),
  output_compression = NULL,
  quality = "high",
  background = "opaque",
  input_fidelity = NULL,
  timeout_seconds = NULL,
  total_timeout_seconds = NULL,
  first_byte_timeout_seconds = NULL,
  connect_timeout_seconds = NULL,
  idle_timeout_seconds = NULL
)
}
\arguments{
\item{x}{A \code{ggplot} object or \code{ggai_session}.}

\item{instruction}{Optional polish direction for the final redraw.}

\item{image_model}{Optional image model identifier.}

\item{registry}{Optional provider registry passed to \code{aisdk}.}

\item{candidate_count}{Number of image candidates to request from the model.}

\item{output_dir}{Output directory for the final images and manifests.}

\item{prefix}{Filename prefix for generated artifacts.}

\item{width, height}{Optional raster dimensions in pixels for the reference
bundle. Defaults to the value returned by \code{ggai_figure_resolution()}.}

\item{dpi}{Raster DPI used when rendering the reference bundle.}

\item{output_format}{Output image format passed through to the image model.}

\item{output_compression}{Optional compression level for JPEG or WebP output.}

\item{quality}{Optional provider-specific quality setting.}

\item{background}{Optional provider-specific background setting.}

\item{input_fidelity}{Optional input fidelity override for providers that
support image editing fidelity controls.}

\item{timeout_seconds}{Legacy alias for \code{total_timeout_seconds}, passed
through to \code{aisdk}.}

\item{total_timeout_seconds}{Optional total request timeout in seconds.}

\item{first_byte_timeout_seconds}{Optional time-to-first-byte timeout in
seconds.}

\item{connect_timeout_seconds}{Optional connection-establishment timeout in
seconds.}

\item{idle_timeout_seconds}{Optional stall timeout in seconds.}
}
\value{
A \code{ggai_polished_figure_result} list.
}
\description{
Uses the ggplot render, geometry overlay, and layout overlay as joint
reference images for a whole-image redraw with an image model.
}
