% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{ggai_execute_and_capture}
\alias{ggai_execute_and_capture}
\title{Execute R code and capture as a \code{ggai_artifact}}
\usage{
ggai_execute_and_capture(
  code,
  env = NULL,
  format = c("png", "svg"),
  engine_hint = NULL,
  output_dir = tempdir(),
  prefix = "artifact",
  width = 1200L,
  height = 900L,
  dpi = 150,
  data_refs = list(),
  provenance = list()
)
}
\arguments{
\item{code}{Character scalar of R code.}

\item{env}{Optional environment in which to evaluate \code{code}. Defaults to a
fresh child of \code{parent.frame()}.}

\item{format}{Output format, one of \code{"png"} or \code{"svg"}.}

\item{engine_hint}{Optional engine override. Use when auto-detection is
known to fail (e.g. \code{grid::grid.draw(grob)} that returns \code{NULL}).}

\item{output_dir}{Output directory. Defaults to \code{tempdir()}.}

\item{prefix}{Filename prefix.}

\item{width, height}{Output dimensions in pixels.}

\item{dpi}{DPI for raster output.}

\item{data_refs}{Optional list to attach to the artifact's \code{data_refs} slot.}

\item{provenance}{Optional list merged into the artifact's \code{provenance} slot.}
}
\value{
A \code{ggai_artifact}.
}
\description{
Evaluates \code{code} in an environment that records both the returned R object
and any base-graphics drawing operations. Detects the figure engine
automatically, renders the result to a file, and returns the artifact.
}
\details{
Engine detection order:
\itemize{
\item composite — \code{last_value} inherits \code{patchwork} (multi-plot composite).
\item ggplot — \code{last_value} inherits \code{ggplot} / \code{gg}.
\item complex_heatmap — \code{last_value} inherits \code{Heatmap} / \code{HeatmapList}.
\item htmlwidget — \code{last_value} inherits \code{htmlwidget}.
\item grid — \code{last_value} inherits \code{grob} / \code{gTree} / \code{gList}.
\item base — the device displaylist contains operations after evaluation.
\item unknown — none of the above.
}

For engines that ggai cannot yet render (complex_heatmap, circlize,
htmlwidget, composite), the artifact is still returned with engine recorded
but \code{rendered} left empty and a warning emitted.
}
